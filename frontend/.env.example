# Vercel / local frontend env (copy to .env.local for local overrides)
# No secrets here — MCP_API_KEY stays in pulse .env / GitHub Actions only.

# Railway hosted Google MCP server (display + dev proxy target)
VITE_MCP_URL=https://mcp-server-production-725c.up.railway.app

# Optional UI overrides (defaults in src/data/mock.ts)
# VITE_CURRENT_WEEK=2026-W25
# VITE_GOOGLE_DOC_URL=https://docs.google.com/document/d/.../edit
# VITE_GITHUB_ACTIONS_URL=https://github.com/SayaliAsole26/groww-review-pulse/actions
# VITE_GMAIL_DRAFT_URL=https://mail.google.com/mail/u/0/#drafts
